<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.seczone.security.mapper.CommentMapper">

  <insert id="InsertComment" parameterType="com.seczone.security.dao.Comment">
      insert into comment(commentid,fromuser,commentcontent,username) values (#{commonId},#{fromUser},#{commentContent},#{userName})
  </insert>
</mapper>